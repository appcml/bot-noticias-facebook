# requirements_video.txt — dependencias exclusivas del video_bot.py
# Instalar con: pip install -r requirements_video.txt

# ── Web / scraping ──────────────────────────────
requests>=2.31.0
feedparser>=6.0.10
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ── Imagen ──────────────────────────────────────
Pillow>=10.0.0

# ── Video — moviepy 1.0.3 (API estable con moviepy.editor) ──
moviepy==1.0.3
imageio>=2.28.0,<2.35.0
imageio-ffmpeg>=0.4.9
numpy>=1.24.0,<2.0.0
decorator>=4.4.2
proglog>=0.1.9
tqdm>=4.0.0

# ── Audio TTS ───────────────────────────────────
edge-tts>=6.1.9
