name: Publicar Noticias Virales

on:
  schedule:
    - cron: '*/30 * * * *'  # Cada 30 minutos
  workflow_dispatch:  # Permite ejecución manual

jobs:
  publicar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
      
    - name: Descargar historial previo
      uses: actions/download-artifact@v4
      with:
        name: historial-noticias
        path: .
      continue-on-error: true
      
    - name: Verificar historial
      run: |
        if [ -f "historial_publicaciones.json" ]; then
          echo "✅ Historial encontrado"
          cat historial_publicaciones.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'📊 {len(d[\\\"urls\\\"])} URLs, {len(d[\\\"titulos\\\"])} títulos')" 2>/dev/null || echo "📄 Archivo existe"
        else
          echo "⚠️ Sin historial previo, creando nuevo"
          echo '{"urls": [], "titulos": [], "ultima_publicacion": null}' > historial_publicaciones.json
        fi
      shell: bash
      
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Instalar dependencias
      run: |
        pip install requests pillow feedparser
        echo "✅ Dependencias instaladas"
        
    - name: Verificar archivos
      run: |
        echo "📁 Archivos:"
        ls -la *.py *.json 2>/dev/null || ls -la
        if [ -f "bot_noticias.py" ]; then
          echo "✅ bot_noticias.py OK"
        else
          echo "❌ bot_noticias.py NO encontrado"
          exit 1
        fi
      shell: bash
      
    - name: Verificar secrets
      run: |
        echo "🔍 Verificando configuración..."
        if [ -z "${{ secrets.FB_PAGE_ID }}" ]; then
          echo "❌ FB_PAGE_ID no configurado"
        else
          echo "✅ FB_PAGE_ID configurado"
        fi
        if [ -z "${{ secrets.FB_ACCESS_TOKEN }}" ]; then
          echo "❌ FB_ACCESS_TOKEN no configurado"
        else
          echo "✅ FB_ACCESS_TOKEN configurado (${#{{ secrets.FB_ACCESS_TOKEN }}} chars)"
        fi
        if [ -z "${{ secrets.DEEPL_API_KEY }}" ]; then
          echo "⚠️ DEEPL_API_KEY no configurado (opcional pero recomendado)"
        else
          echo "✅ DEEPL_API_KEY configurado"
        fi
      shell: bash
        
    - name: Ejecutar bot
      env:
        NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        GNEWS_API_KEY: ${{ secrets.GNEWS_API_KEY }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}
        FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
        FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
      run: |
        echo "🚀 Iniciando bot..."
        python bot_noticias.py
      shell: bash
      
    - name: Verificar historial actualizado
      if: always()
      run: |
        if [ -f "historial_publicaciones.json" ]; then
          echo "📊 Historial final:"
          cat historial_publicaciones.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'URLs: {len(d[\\\"urls\\\"])}, Última: {d.get(\\\"ultima_publicacion\\\",\\\"N/A\\\")[:19] if d.get(\\\"ultima_publicacion\\\") else \\\"N/A\\\"}')" 2>/dev/null || echo "✅ Archivo actualizado"
        fi
      shell: bash
      
    - name: Guardar historial
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: historial-noticias
        path: historial_publicaciones.json
        retention-days: 90
        overwrite: true
